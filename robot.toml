output-dir="./tests_robot/results"
languages = ["en"]
default-profiles = "worker"
skip-teardown-on-exit = true


[profiles.worker]
variables = { TARGET = "url", URL = { expr="'/dev/ttyUSB0' if platform.system() == 'Linux' else 'COM22'" }}
excludes = [ "descale" ]

[profiles.descaler]
variables = { TARGET = "url", URL = { expr="'/dev/ttyUSB0' if platform.system() == 'Linux' else 'COM22'" }}
excludes = [ "worker" ]

[profiles.simulation_mvp]
variables = { TARGET = "simulation", SIMULATION_EXE_PATH = { expr="Path(environ.get('FIRMWARE_BUILD_DIR_PATH')) / 'linux/mvp_simulation/src/simulation/cli_simulation_mvp/cli_simulation_mvp'"} }
excludes = [ "descale" ]

[profiles.simulation_descale]
variables = { TARGET = "simulation", SIMULATION_EXE_PATH = { expr="Path(environ.get('FIRMWARE_BUILD_DIR_PATH')) / 'linux/mvp_simulation_descale/src/simulation/cli_simulation_descale/cli_simulation_descale'"} }
excludes = [ "worker" ]